{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@google/genai": "^1.0.0",
    "express": "^5.1.0",
    "mongoose": "^8.15.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.2",
    "@types/node": "^22.15.19",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "dev": "docker-compose up -d; ts-node-dev --respawn index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "clear": "docker stop ChatBot",
    "clearHard": "docker stop $(docker ps -a -q)"
  }
}
